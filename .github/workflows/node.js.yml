name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  build-update:
    runs-on: self-hosted
    steps:
      - run: npm install
      - run: npx expo update --channel preview --message "${{github.event.head_commit.message}}"
